#include <stdio.h>

int main() {
    // condition ? expression-if-true : expression-if-false 
    int a = 350, b = 390;
    a>b? printf("A is Greater, A is %d ", a): printf("Not B is Geater, B is %d", b);
    return 0;
}