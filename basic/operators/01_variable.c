#include <stdio.h>

int main() {
    int i = 10; // Delcare and initialize i with 10
    int j = i;
    int a = 2, b = 3, c = 4, d = 5;
    int j1 = a +j -i;
    printf("The Value of i is %d and j is %d\n", i,j);
    printf("The Value of a is %d and b is %d\n", a,b);
    printf("The Value of c is %d and d is %d\n", c,d);
    printf("%d",j1);
    return 0;
}